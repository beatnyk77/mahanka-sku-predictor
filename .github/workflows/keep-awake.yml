name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes
  workflow_dispatch:  # Manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: curl -s -o /dev/null https://mahanka-sku-predictor-d94l6jjzmoqp9nbdqkswe4.streamlit.app/
